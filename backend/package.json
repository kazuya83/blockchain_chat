{
  "name": "@blockchain-chat/backend",
  "private": true,
  "type": "module",
  "workspaces": [
    "chat"
  ],
  "scripts": {
    "dev": "npm run dev -w chat",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "start": "npm run start -w chat",
    "db:pull": "npm run db:pull -w chat",
    "db:generate": "npm run db:generate -w chat"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "prisma": "^7.9.1",
    "tsup": "^8.5.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  }
}
